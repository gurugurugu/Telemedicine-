@{
    // 在这里添加用户名和角色信息到二维码中
    string username = "JohnSmith";
    string role = "Doctor";
    string qrData = $"{username},{role}";
}

<div class="text-center">
    <!-- 显示二维码 -->
    <img src="@Url.Action("GenerateQRCode", "QRCode", new { qrData })" alt="QR Code" />
</div>